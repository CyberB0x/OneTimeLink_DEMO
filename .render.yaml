services:
  - type: web
    name: onetimelink-demo
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn onetimelink_demo.wsgi"
    envVars:
      - key: DEBUG
        value: "False"
